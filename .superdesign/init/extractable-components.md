# Extractable Components

No reusable components exist yet. The entire page is one monolithic App.tsx.

## Layout Components
None — nav is inline in App.tsx.

## Basic Components
None — buttons, links are inline Tailwind classes in App.tsx.

## Candidates for Extraction (future)
- **NavBar** — top navigation with brand, desktop links, mobile hamburger → sheet
- **HeroSection** — heading with inline icons, subtext, CTA button
- **MobileSheet** — slide-in menu with nav links and action buttons
