# Shared UI Components

No shared UI component library exists. The dashboard is a single-page landing page with all UI rendered inline in App.tsx. Components directory is empty.

## Icons Used
- Lucide React: Zap, LockKeyhole, Fingerprint, ArrowRightCircle, Menu, X
